---
name: minimal-structural-collage
description: 将任意照片制作成纵向 2:3 的“上半原始实拍、下半抽象纸本结构构成”拼贴。用于用户提到极简结构拼贴、像素蛋糕式拼贴、照片加高留白抽象构成、上实拍下纸本，或要求复刻这种高级简约版式时；尤其适合必须保持人物、食物、花卉和生活照片原始质感与比例的任务。
---

# 极简结构拼贴

把参考照片转成一张完整的纵向 2:3 拼贴：上半区是未经 AI 重绘的原照片，下半区是从照片的重心、方向、负形和色彩关系提炼出的高留白纸本结构构成。把“原图保真”和“艺术生成”分开处理。

## 开始前

- 读取 [references/style-spec.md](references/style-spec.md) 了解固定版式、配色和不同题材的抽象规则。
- 生成艺术草案或用户需要交给其他 Agent / AI 的文字提示词时，读取 [references/universal-prompt.md](references/universal-prompt.md)。
- 一张输入图对应一张独立输出。除非用户明确要求，不要把多张照片拼进同一张成品。

## 不可妥协的结果

- 画布严格为纵向 `2:3`；默认 `1024 × 1536 px`。
- 上下各占约 `50%`，默认分界线为 `y=768`；默认横向满幅、干净硬切，不使用厚边框。只有为了完整保留主体而使用 `contain` 时，才允许暖纸色空边。
- 上半主体必须来自原图的确定性等比缩放 / 裁切：不得让生成模型重画、磨皮、锐化、调色或改变人物与物体比例。`extend` 模式只能生成外围背景，原主体或原始中央照片区域仍须回贴。
- 下半区使用暖象牙色纸张、高留白、少量不完美的丝网印刷 / 干刷 / 撕纸质感；不是普通滤镜，也不是照片描边。
- 不拉伸照片，不用镜像延展制造重复门框、建筑线或肢体。
- 不添加水印、平台标识、账号、日期、边框、贴纸或大标题。

## 工作流

### 0. 解析运行路径

- 所有相对资源都以本 `SKILL.md` 所在目录作为 `<skill-root>`，不要假设当前工作目录就是 Skill 目录。
- 在 Codex Desktop 中先读取工作区依赖，使用其中返回的 Python executable 作为 `<python>`；在其他环境中使用可用的 `python3`、`python` 或 `py`。
- 脚本需要 Python 3.10+ 与 Pillow（含 `ImageCms`）。排中文文案时必须显式传入支持中文的字体，例如思源宋体 / Noto Serif CJK。
- 下文命令中的 `<python>` 与 `<skill-root>` 都是待解析占位符，不要原样执行。

### 1. 检查原图并规划裁切

先识别主体、动作、视线、手臂、随身物与背景中的关键结构。为上半幅选择不会切坏主体的 `focus-x` 与 `focus-y`：

- 人像优先保住头部、肩线、手势与能解释姿态的手臂；不要只剩一张放大的脸。
- 食物和物件优先保住完整轮廓、容器边缘与关键道具。
- 花卉和静物优先保住主花 / 主物与必要的环境关系。

上半构图按以下顺序选择：

1. `cover`：在不破坏主体时首选，通过焦点参数移动裁切窗；
2. `extend`：若裁切会损失必要手臂、身体或完整物件，要求生成草案的上半仅做背景延展，再把完整原图确定性回贴；
3. `contain`：背景不适合延展时的最后兜底，允许暖纸色空边。

任何模式都不得拉伸或用镜像补满。

如果输入是 MPO、方向异常的手机 JPG，或生成工具拒绝读取，先运行：

```text
<python> "<skill-root>/scripts/normalize_images.py" "<output-dir>" "<image-1>" ["<image-2>" ...]
```

### 2. 生成艺术草案

使用可用的图像生成 / 编辑工具，并把原图作为内容参考。每张图单独生成一次完整的 `1024 × 1536` 草案，以便下半结构构成能呼应上半构图。

提示模型遵守以下重点：

- 上半区只作为构图占位参考，最终会被原图覆盖。
- 先抽象“关系”，不要先画“物体”：按重心位置、方向轴、面积比例、层叠遮挡、负形与色彩节奏排序。完整物件轮廓、完整人物和五官都不是默认锚点。
- 用三级尺度组织画面：一个大的负形 / 未闭合框架，`3–6` 个中尺度块面，`1–3` 个微小点状或短线强调；只有必要时保留一个占下半宽度约 `8–12%` 的微型具象线索。
- 先留白，再定关系骨架，最后才加纹理；底图约 `60–75%` 保持连续、安静的留白。下半脱离上图后若仍像一幅完整的物件或人物插画，说明过于具象，必须重做。
- 只用 `4–6` 种低饱和颜色；从原图抽取一个重点色，其余使用暖米白、炭黑、驼棕等中性色。
- 近景人像必须主动缩小并符号化为胸像 / 半身结构：皮肤保留纸色，只用少量断裂线提示脸朝向，禁止完整眼睛、虹膜、睫毛、鼻部塑形、唇部高光和写实肤色明暗。
- 默认保留一则小幅两行英文衬线文案；只有用户明确要求无字时才省略。不要让图像模型画字：先生成无字底图，再用 `scripts/add_caption.py` 排入拼写准确的真实文字。

可直接从 [references/universal-prompt.md](references/universal-prompt.md) 的“一次生成版”取提示词，并按照片内容替换变量。

### 3. 用原图重新组装上半区

不要直接交付模型生成的上半照片。运行确定性合成脚本：

```text
<python> "<skill-root>/scripts/assemble_collage.py" "<original>" "<generated-draft>" "<output.png>" --focus-x 0.5 --focus-y 0.5
```

默认行为：

- 从草案提取下半区；
- 对原图做等比 `cover` 裁切并放入上半区；
- 不执行锐化、降噪、美颜、重绘或额外调色；
- 输出无损 PNG，尺寸固定为 `1024 × 1536`。

若需要背景延展，草案必须是完整 2:3，而且上半外围只能包含背景延展、不能出现重复主体：

```text
<python> "<skill-root>/scripts/assemble_collage.py" "<original>" "<generated-draft>" "<output.png>" --top-mode extend --artwork-mode full
```

背景不适合延展、但必须保留完整照片时，最后改用：

```text
<python> "<skill-root>/scripts/assemble_collage.py" "<original>" "<generated-draft>" "<output.png>" --top-mode contain
```

只拿到独立下半构成时，追加 `--artwork-mode panel`。草案是完整 2:3 拼贴时使用默认 `auto` 或显式 `--artwork-mode full`。

### 3.5 排入英文小字

先按第 4 节用 `--artwork` 验证无字合成底图。除非用户明确要求无字，再拟一则与场景有关、含蓄安静的两行英文短句，共 `5–10` 个词；不要直白命名物体。然后在组装后的成品上运行：

```text
<python> "<skill-root>/scripts/add_caption.py" "<output.png>" "<output.png>" --line1 "First line" --line2 "second line."
```

脚本会在下半区候选留白中选择干扰最少的位置，并使用真实小号衬线字体排版。若用户提供了文案，逐字使用；若用户明确指定位置，追加 `--position lower-left` 等参数。字体不可用时用 `--font "<serif-font.ttf>"` 指定经典衬线字体。最终视觉检查仍要确认文案没有压住主体。

### 4. 验证再交付

排字前，对无字合成底图运行完整来源验证：

```text
<python> "<skill-root>/scripts/verify_collage.py" "<output.png>" --original "<original>" --artwork "<generated-draft>" --focus-x 0.5 --focus-y 0.5
```

验证时必须复用组装时的全部非默认参数，包括 `--top-mode`、`--artwork-mode`、`--focus-x/y`、`--split`、`--paper-color` 以及 `--allow-crop-artwork`；否则会把正确成品误报为失败。

排字后，文字会使下半区不再与无字草案逐像素相同，因此最终成品只复验尺寸与上半来源，不传 `--artwork`：

```text
<python> "<skill-root>/scripts/verify_collage.py" "<output.png>" --original "<original>" --focus-x 0.5 --focus-y 0.5
```

这一步同样要复用组装时的 `--top-mode`、`--focus-x/y`、`--split` 和 `--paper-color`。

再做一次视觉检查：

- 尺寸与分界是否准确；
- 上半主体是否自然、没有拉伸，且保留原始皮肤 / 毛发 / 食物细节；
- 手臂、手、手机、碗沿等是否仍符合原图比例；
- 缩略图下，下半是否首先读作抽象编辑构成，并通过重心、方向、色彩和空间节奏与上半呼应；若先读成完整的碗、花、相机、购物袋或写实人像，则未通过；
- 近景人像是否已降尺度、去五官塑形、去肤色明暗；第一眼若仍是“大脸肖像”而不是“结构与留白”，则未通过；
- 留白是否足够、装饰是否克制；英文小字是否存在（除非用户要求无字）、拼写正确，且没有压住主体；
- 是否出现多余肢体、重复物件、伪文字、水印或 UI。

只要上半区被 AI 改过，即使整体“更漂亮”，也视为未通过。重新合成，不要继续用提示词修脸。

## 批量规则

- 对每张照片分别判断主体与裁切位置，不共用一个固定 `focus-y`。
- 保持统一的纸张、颗粒、字级与上下比例；根据每张照片切换版式重心和强调色。
- 批量完成后逐张验证，不能只抽查第一张。

## 交付

- 优先交付无损 PNG，并在回复中直接展示成品。
- 简要说明采用的主体锚点、强调色，以及上半图已由原图确定性回贴。
- 若用户还要复用提示词，同时提供 [references/universal-prompt.md](references/universal-prompt.md)，不要只贴一段难以维护的聊天文本。
